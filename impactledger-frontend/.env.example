# Copy to .env and set your values.
# API base URL (no trailing slash). Frontend appends paths like /auth/register, /voice/transcribe.
# - Local dev with proxy: /api  (vite proxies to localhost:8080)
# - Deployed backend: https://impactledger-backend.onrender.com/api
VITE_API_URL=/api
